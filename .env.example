# Supabase public configuration
# Never commit the service-role key.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Private server-side admin credentials — never expose these to the browser.
ADMIN_PASSWORD=
# Server-only Supabase secret key used by admin write actions.
SUPABASE_SECRET_KEY=
