import type {Metadata} from 'next'
export const metadata:Metadata={title:'Photothèque du Niger',description:'Photographies documentaires du Niger avec lieux, crédits, sources et informations de licence.'}
export default function Layout({children}:{children:React.ReactNode}){return children}
