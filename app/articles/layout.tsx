import type {Metadata} from 'next'
export const metadata:Metadata={title:'Histoires et articles sur le Niger',description:'Récits, histoire, patrimoine, culture et analyses documentaires consacrés au Niger.'}
export default function Layout({children}:{children:React.ReactNode}){return children}
