import type {Metadata} from 'next'
export const metadata:Metadata={title:'Contribuer au projet',description:'Proposez une découverte, une tradition, une histoire, une image ou un événement à l’équipe éditoriale.'}
export default function Layout({children}:{children:React.ReactNode}){return children}
