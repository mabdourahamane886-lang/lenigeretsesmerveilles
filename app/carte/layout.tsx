import type {Metadata} from 'next'
export const metadata:Metadata={title:'Carte du Niger',description:'Explorez les huit régions du Niger et les contenus géographiques publiés sur la plateforme.'}
export default function Layout({children}:{children:React.ReactNode}){return children}
