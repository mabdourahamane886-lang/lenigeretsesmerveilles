import type {Metadata} from 'next'
import {createClient} from '../../../lib/supabase/server'
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const supabase=await createClient();const {data}=supabase?await supabase.from('niger_wonders').select('name,short_description,description,cover_url').eq('slug',slug).eq('published',true).maybeSingle():{data:null};return {title:data?.name||'Merveille du Niger',description:data?.short_description||data?.description||'Patrimoine et paysages du Niger.',openGraph:{title:data?.name||'Merveille du Niger',description:data?.short_description||data?.description||'Patrimoine du Niger',images:data?.cover_url?[{url:data.cover_url}]:undefined}}}
export default function Layout({children}:{children:React.ReactNode}){return children}
