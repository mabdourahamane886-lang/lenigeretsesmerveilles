import type {Metadata} from 'next'
export const metadata:Metadata={title:'Culture et traditions du Niger',description:'Langues, peuples, traditions, histoire, artisanat et mémoire culturelle du Niger.'}
export default function Layout({children}:{children:React.ReactNode}){return children}
